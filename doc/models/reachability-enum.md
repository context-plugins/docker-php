
# Reachability Enum

Reachability represents the reachability of a node.

## Enumeration

`ReachabilityEnum`

## Fields

| Name |
|  --- |
| `UNKNOWN` |
| `UNREACHABLE` |
| `REACHABLE` |

## Example

```php
use DockerLib\Models\ReachabilityEnum;

$reachability = ReachabilityEnum::UNREACHABLE;
```

