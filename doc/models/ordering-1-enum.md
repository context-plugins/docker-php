
# Ordering 1 Enum

Orders the results by this property.

Prefixing with `-` sorts by descending order.

## Enumeration

`Ordering1Enum`

## Fields

| Name |
|  --- |
| `LAST_ACTIVITY` |
| `ENUM_LAST_ACTIVITY` |
| `DIGEST` |
| `ENUM_DIGEST` |

## Example

```php
use DockerLib\Models\Ordering1Enum;

$ordering1 = Ordering1Enum::DIGEST;
```

