<?php

declare(strict_types=1);

/*
 * DockerLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

namespace DockerLib\Tests;

use Core\Types\CallbackCatcher;
use DockerLib\DockerClient;
use DockerLib\DockerClientBuilder;

class ClientFactory
{
    public static function create(CallbackCatcher $httpCallback): DockerClient
    {
        $clientBuilder = DockerClientBuilder::init();
        $clientBuilder = self::addConfigurationFromEnvironment($clientBuilder);
        $clientBuilder = self::addTestConfiguration($clientBuilder);
        return $clientBuilder->httpCallback($httpCallback)->build();
    }

    public static function addTestConfiguration(DockerClientBuilder $builder): DockerClientBuilder
    {
        return $builder;
    }

    public static function addConfigurationFromEnvironment(DockerClientBuilder $builder): DockerClientBuilder
    {
        $timeout = getenv('DOCKER_LIB_TIMEOUT');
        $numberOfRetries = getenv('DOCKER_LIB_NUMBER_OF_RETRIES');
        $maximumRetryWaitTime = getenv('DOCKER_LIB_MAXIMUM_RETRY_WAIT_TIME');
        $environment = getenv('DOCKER_LIB_ENVIRONMENT');
        $version = getenv('DOCKER_LIB_VERSION');

        if (!empty($timeout) && \is_numeric($timeout)) {
            $builder->timeout(intval($timeout));
        }

        if (!empty($numberOfRetries) && \is_numeric($numberOfRetries)) {
            $builder->numberOfRetries(intval($numberOfRetries));
        }

        if (!empty($maximumRetryWaitTime) && \is_numeric($maximumRetryWaitTime)) {
            $builder->maximumRetryWaitTime(intval($maximumRetryWaitTime));
        }

        if (!empty($environment)) {
            $builder->environment($environment);
        }

        if (!empty($version)) {
            $builder->version($version);
        }

        return $builder;
    }
}
